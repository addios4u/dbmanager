# F5 Refresh in TableDataView Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 테이블 뷰에서 F5 키로 데이터를 새로고침하되, 현재 정렬·WHERE 필터·페이지 오프셋을 유지한다.

**Architecture:** `TableDataView` 컴포넌트 내부에 `handleRefresh` 콜백을 추출하고, 기존 Refresh 버튼과 새 F5 keydown 리스너가 이 콜백을 공유한다. 웹뷰 iframe이 이벤트를 먼저 캡처하므로 VS Code의 F5(Start Debugging)와 충돌하지 않는다.

**Tech Stack:** React, TypeScript, Zustand (tableData store)

---

## File Map

| 파일 | 변경 유형 | 내용 |
|------|-----------|------|
| `packages/webview-ui/src/components/TableDataView/index.tsx` | Modify | `handleRefresh` 추출, 버튼 교체, F5 keydown useEffect 추가 |

---

### Task 1: `handleRefresh` 콜백 추출 및 Refresh 버튼 교체

**Files:**
- Modify: `packages/webview-ui/src/components/TableDataView/index.tsx`

- [ ] **Step 1: `handleRefresh` 콜백 추가**

`fetchData` 콜백 정의 바로 다음(line 92 이후)에 아래 코드를 삽입한다.

```ts
const handleRefresh = useCallback(() => {
  const whereChanged = whereClause !== appliedWhere;
  setAppliedWhere(whereClause);
  fetchData({
    offset: whereChanged ? 0 : offset,
    where: whereClause,
  });
}, [whereClause, appliedWhere, offset, fetchData]);
```

- [ ] **Step 2: Refresh 버튼의 onClick을 `handleRefresh`로 교체**

`TableDataView/index.tsx` 에서 아래 부분을 찾는다 (line ~499):

```tsx
onClick={() => fetchData({ offset })}
```

아래로 교체한다:

```tsx
onClick={handleRefresh}
```

- [ ] **Step 3: 타입체크 통과 확인**

```bash
pnpm typecheck
```

Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add packages/webview-ui/src/components/TableDataView/index.tsx
git commit -m "refactor: extract handleRefresh callback in TableDataView"
```

---

### Task 2: F5 keydown 리스너 추가

**Files:**
- Modify: `packages/webview-ui/src/components/TableDataView/index.tsx`

- [ ] **Step 1: F5 keydown useEffect 추가**

기존 useEffect 블록들(importProgress 리스너 등) 아래에 추가한다.

```ts
// F5 새로고침
useEffect(() => {
  const handler = (e: KeyboardEvent) => {
    if (e.key !== 'F5') return;
    if (editingCell || showDeleteConfirm || statusError) return;
    e.preventDefault();
    handleRefresh();
  };
  window.addEventListener('keydown', handler);
  return () => window.removeEventListener('keydown', handler);
}, [editingCell, showDeleteConfirm, statusError, handleRefresh]);
```

- [ ] **Step 2: 타입체크 통과 확인**

```bash
pnpm typecheck
```

Expected: 에러 없음

- [ ] **Step 3: 빌드 확인**

```bash
pnpm --filter @dbmanager/webview-ui build
```

Expected: 에러 없이 빌드 완료

- [ ] **Step 4: 커밋**

```bash
git add packages/webview-ui/src/components/TableDataView/index.tsx
git commit -m "feat: add F5 keyboard shortcut to refresh table data"
```

---

### Task 3: 수동 검증

- [ ] **Step 1: Extension Development Host 실행**

VS Code에서 F5 → "Extension Development Host" 창 열림

- [ ] **Step 2: 기본 새로고침 동작 확인**

1. DBManager에서 테이블 뷰 열기
2. 컬럼 클릭해 정렬 적용
3. WHERE 입력창에 조건 입력 후 Apply
4. 다른 페이지로 이동
5. F5 → 같은 정렬, 같은 WHERE, 같은 페이지로 새로고침되는지 확인

- [ ] **Step 3: 미적용 WHERE 동작 확인**

1. WHERE 입력창에 새 조건 타이핑 (Apply 누르지 않음)
2. F5 → WHERE가 적용되고 offset 0(첫 페이지)으로 리셋되는지 확인

- [ ] **Step 4: 모달 가드 확인**

1. 셀 더블클릭 → Edit Cell 다이얼로그 열기
2. F5 → 새로고침되지 않고 다이얼로그 유지 확인

- [ ] **Step 5: 버튼과 F5 동일 동작 확인**

Refresh 버튼과 F5가 동일하게 동작하는지 비교 확인
